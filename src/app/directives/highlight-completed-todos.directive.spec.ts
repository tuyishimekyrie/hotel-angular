import { HighlightCompletedTodosDirective } from './highlight-completed-todos.directive';

describe('HighlightCompletedTodosDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightCompletedTodosDirective();
    expect(directive).toBeTruthy();
  });
});
